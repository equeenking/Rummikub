version: '3.8'

services:
  rummikub:
    build: .
    container_name: rummikub-server
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - rummikub-network

networks:
  rummikub-network:
    driver: bridge